# Group-Maker--Program-
The Student Group Generator is a Python program that randomly creates student groups based on a user-selected group size. The program allows the user to generate multiple group configurations (such as groups of 2, 3, or 4) without restarting the program. It also supports custom exception rule
